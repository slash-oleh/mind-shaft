# Jira: comment

Requires `cloudId` - resolve via `getAccessibleAtlassianResources` (or reuse a value already known this session).

Call `addCommentToJiraIssue` with `issueIdOrKey: TICKET_ID` and `commentBody` set to the contents of `BODY_FILE`.
