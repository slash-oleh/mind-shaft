{
  "name": "mind-shaft",
  "version": "1.0.0",
  "description": "Development handbook monorepo.",
  "repository": "https://github.com/slash-oleh/mind-shaft",
  "workspaces": [
    "packages/*"
  ],
  "bin": {
    "mind-shaft": "packages/cli/bin/install.js"
  },
  "scripts": {
    "lint": "npm run rules:lint && npm run skills:lint",
    "lint:fix": "npm run lint:fix -w @mind-shaft/rules && npm run lint:fix -w @mind-shaft/skills",
    "build:docs": "zensical build --clean",
    "generate": "npm run rules:generate:ai && npm run skills:generate:ai",
    "rules:lint": "npm run lint -w @mind-shaft/rules",
    "skills:lint": "npm run lint -w @mind-shaft/skills",
    "spellcheck": "cspell lint \"**/*.{md,js,ts,json,yml}\"",
    "rules:generate:ai": "npm run build:ai -w @mind-shaft/rules && rm -rf ai/rules && mkdir -p ai/rules && cp -r packages/rules/dist/ai/* ai/rules/",
    "skills:generate:ai": "npm run build:ai -w @mind-shaft/skills && rm -rf ai/skills && mkdir -p ai/skills && cp -r packages/skills/dist/ai/* ai/skills/",
    "agents:sync": "rulesync install && rulesync generate"
  },
  "type": "module",
  "dependencies": {
    "tar": "^7.4.3"
  },
  "devDependencies": {
    "cspell": "^10.0.0",
    "rulesync": "^12.0.0"
  }
}
