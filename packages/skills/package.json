{
  "name": "@dev-handbook/skills",
  "version": "1.0.0",
  "description": "Processes.",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && rsync -a --exclude='README.md' src/ dist/"
  }
}
