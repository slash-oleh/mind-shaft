# Jira: get

Call `getJiraIssue` with `cloudId`, `issueIdOrKey: TICKET_ID`, and `responseContentFormat: "markdown"`.

The default response covers every abstract field except Parent and custom fields. Pass those as `fields` when needed.
