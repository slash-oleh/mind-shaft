---
title: Home
---

# vcs-tools

For skill itself see [SKILL.md](./SKILL.md).

## Meta info

Provider layer for PR/MR operations - the counterpart to `ticket-tools` on the tracker side. Every calling skill needs the same PR data and actions; without this, each would hardcode `gh` vs `glab` itself.
