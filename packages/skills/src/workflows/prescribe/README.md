---
title: Home
---

# prescribe

For skill itself see [SKILL.md](./SKILL.md).

## Meta info

`elaborate`'s counterpart for bug tickets: chains `investigate` -> `clarify` -> `spec` instead of `confront` -> `clarify` -> `spec`.
